# frozen_string_literal: true

RSpec.describe GA4::Events::Sender do
  it "has a version number" do
    expect(GA4::Events::Sender::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
